try {
    (Get-Content package.json -Raw | ConvertFrom-Json) | ConvertTo-Json -Depth 100 | Set-Content package.json
    Write-Host "✅ package.json validated and formatted" -ForegroundColor Green
} catch {
    Write-Host "❌ Invalid JSON in package.json" -ForegroundColor Red
    Write-Host $_.Exception.Message -ForegroundColor Yellow
}
